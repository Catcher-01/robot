# 医院导诊机器人系统项目报告

**项目成员：黄云辉　宋文轩　张睿琦**
**指导教师：刘岩**
**课程名称：机器人系统设计综合实践**
**报告日期：2026年6月**

---

## 一、项目概述

### 1.1 项目背景与意义

随着我国医疗服务需求的持续增长，医院内部物资配送与患者导诊面临着效率与安全的双重挑战。传统人工方式存在成本高、响应慢、易交叉感染等固有缺陷，而自主移动机器人（AMR）能够在降低运营成本的同时提升院内服务效率，是智慧医院建设的重要组成部分。然而，在真实医院环境中直接部署机器人存在较高的试错成本，因此先在仿真平台中完成技术验证与方案迭代，是一条可行且高效的研发路线。

本项目以**医院智能导诊**为核心应用场景，构建了一套融合语音交互、手势识别、自然语言理解和自主导航的智能机器人系统。系统以 ROS 2 Humble 为通信骨干，以 Gazebo 11 为仿真引擎，以 TurtleBot3 为机器人平台，实现了从环境感知、定位、路径规划、运动控制到多模态人机交互的完整功能闭环。

### 1.2 项目目标

本项目设定了以下核心目标：

**感知与环境建模层面**，利用 TurtleBot3 waffle_pi 所配备的 LDS 激光测距传感器（360° 全向扫描，12m 测距范围）实现环境感知，构建 2D 占据栅格地图，并通过 AMCL（自适应蒙特卡洛定位）算法实现机器人在已知环境中的实时定位。

**运动控制与避障层面**，基于 Nav2 导航栈实现机器人的基本运动控制，设计并对比反应式（DWA 动态窗口法）与基于规划（TEB）两类避障策略在实际场景中的表现差异。

**路径规划层面**，集成 A* 全局路径规划与 DWA/TEB 局部规划算法，结合 Nav2 导航栈进行调优，分析不同算法在复杂医院环境中的适用条件与性能差异。

**应用场景层面**，设计并实现病房送药、巡逻巡检两个独立任务场景，每个场景包含明确的任务目标与量化评估指标。

**人机交互层面**，通过触控界面、语音交互和手势识别三种模态实现机器人与患者的自然交互，并集成大语言模型（DeepSeek）实现智能导诊功能。

**系统部署层面**，在 Gazebo 仿真环境中完成全部功能的开发与验证，并分析从仿真环境向真实物理机器人迁移的技术障碍与应对策略。

---

## 二、系统总体架构

### 2.1 架构设计原则

本系统采用**五层分层架构**，各层职责明确、边界清晰，层间通过标准化接口通信，耦合度低，便于独立开发、测试与维护。自顶向下依次为：用户交互层、业务逻辑层、导航决策层、感知层和基础设施层。

这种分层设计具有以下优势：各功能模块可以独立演进，降低了系统集成和调试的复杂度；每个层次的技术选型可以针对其特定需求进行优化，而不必受其他层次约束；系统的可测试性大大提高，可以在各层之间使用模拟（Mock）数据进行独立验证。

### 2.2 各层职责概述

**用户交互层**面向最终用户提供多模态交互接口，包括 1280×800 竖屏触控界面、语音交互（唤醒 → 录音 → 识别 → 意图解析 → TTS 反馈）和手势识别（MediaPipe 手部关键点检测 → 鼠标/触控事件）。该层完全独立于 ROS 环境运行，通过进程间通信与业务逻辑层交互。

**业务逻辑层**包含四个核心模块。`hospital_gui` 以 PyQt5 为框架，实现 5 个页面的栈式切换（首页、目的地确认页、导航执行页、到达完成页、网页内嵌页）和全局紧急求助按钮，通过 ROS Bridge 线程连接 ROS 2 导航栈。`voice_node` 以 faster-whisper 为 ASR 引擎、DeepSeek API 为意图解析后端、edge-tts 为语音合成引擎，实现完整的语音交互流水线。`nav_controller` 作为 ROS 节点，订阅语音意图话题并将目标点坐标发送给 Nav2 导航栈。`gesture_node` 基于 MediaPipe HandLandmarker 实现手势到鼠标操作的映射。

**导航决策层**以 Nav2 导航栈为核心，通过 NavigateToPose Action 接口接收目标点位姿，由全局规划器（A*）计算最短路径，再由局部规划器（DWA 或 TEB）生成实时避障轨迹，最终通过 /cmd_vel 话题向机器人底驱控制器发送速度指令。导航任务的生命周期通过 Nav2 的 Action 接口管理，支持任务取消、暂停和状态反馈等高级特性。

**感知层**通过 ROS 2 话题接收和处理传感器数据。关键话题包括：/scan（LaserScan 激光雷达扫描数据）、/amcl_pose（机器人在地图中的实时位姿）、/map（OccupancyGrid 占据栅格地图）和 /plan（nav_msgs/Path 全局规划路径）。所有话题数据通过 DDS（Data Distribution Service）中间件在 ROS 节点之间实时分发。

**基础设施层**由 ROS 2 Humble 通信中间件、Gazebo 11 物理仿真引擎和 TurtleBot3 机器人模型三部分组成。ROS 2 提供了话题（Topic）、服务（Service）和动作（Action）三种通信机制，支持分布式部署；Gazebo 11 提供了高保真度的物理仿真能力，包括刚体碰撞检测、摩擦力模拟和传感器噪声建模；TurtleBot3 waffle_pi 提供了完整的机器人模型描述文件（URDF）。

### 2.3 进程间通信拓扑

系统涉及四个独立运行进程的协调工作，通过 ROS 2 DDS 网络实现进程间通信：

`hospital_gui` 进程作为独立 GUI 应用运行，包含三个内部线程：PyQt5 主线程负责 UI 渲染和用户交互；ROS Bridge 线程运行 rclpy spin_once（50ms 周期），订阅 /amcl_pose、/plan、/scan、/map、/voice_to_nav 等话题，并通过 Qt QueuedConnection 信号将数据传递给主线程；语音线程通过 record_until_silence 接口直接调用 voice_node 的离线引擎，完成录制→ASR→意图解析的全流程。GUI 进程通过 NavigateToPose Action Client 向 Nav2 导航栈发送目标指令。

`voice_node` 进程作为 ROS 2 节点运行（或以离线引擎形式被 GUI 直接导入），包含麦克风监听线程（持续检测唤醒词）、ASR 推理线程（faster-whisper small，CUDA/CPU 加速）和 TTS 合成线程（edge-tts XiaoxiaoNeural 音色）。意图解析结果通过 /voice_to_nav 话题（std_msgs/String，JSON 格式）发布到 DDS 网络。

`nav_controller` 进程订阅 /voice_to_nav 话题，接收 JSON 格式的意图消息后提取目的地坐标，调用 NavigateToPose Action 将导航目标发送给 Nav2 导航栈。

`gesture_node` 进程通过 USB 摄像头（/dev/video0，640×480 MJPG）捕获视频流，以 20Hz 帧率运行 MediaPipe HandLandmarker，检测到手势动作后通过 /gesture_command 话题发布 JSON 格式的手势事件。

---

## 三、技术实现详解

### 3.1 仿真环境构建

仿真环境基于 AWS RoboMaker 医院世界模型构建，该模型包含完整的医院建筑结构：病房、走廊、电梯、床位、医疗设备等。利用 Gazebo 11 的 ray cast 插件模拟激光雷达感知，通过 gazebo_map_creator 插件实现地图的实时生成。

地图生成采用两种方案互补：**离线 SDF 解析**通过 Python 脚本 `gen_hospital_map.py` 直接解析医院世界的 SDF 文件，提取静态几何体的位置和形状数据，生成 2D 占据栅格地图，执行速度快，适合开发初期快速迭代；**Gazebo 插件实时生成**在仿真运行时执行射线投射，能够捕获仿真环境中的精确几何信息和传感器视角，生成的地图与实际导航时的感知视角更为一致。

已成功生成的地图文件包括：`hospital_map.pgm`（2D 占据栅格地图，Portable Graymap 格式）、`hospital_map.yaml`（地图元数据）、`hospital_map.pcd`（3D 点云地图）和 `hospital_map.png`（可视化预览）。地图覆盖范围为 35m×35m，分辨率为 0.05m/像素。

### 3.2 导航系统实现

Nav2 导航栈的配置经过系统性调优以适配医院环境特点。**全局规划器**采用 A* 算法，启发式函数选用对角距离（适用于医院规则的网格化走廊结构），相比欧几里得距离可减少约 30% 的搜索节点数。**局部规划器**支持 DWA 和 TEB 两种策略的切换，DWA 以计算快速著称（单次规划周期约 10-20ms），适合算力受限场景；TEB 以路径质量见长（偏离全局路径距离 < 0.18m），但计算量更大（约 50-200ms）。

**AMCL 定位**采用自适应蒙特卡洛算法，粒子数在 500-3000 范围内自适应调整，KLD 误差阈值设为 0.05。机器人模型采用 URDF 描述文件定义各连杆的几何参数和惯性参数，通过 robot_state_publisher 实时发布 TF 变换树，描述了机器人各部件之间的空间变换关系。

**代价地图**采用层叠式架构，包含静态地图层（来自 map_server）、障碍物层（来自激光雷达）、膨胀层（为安全裕度膨胀障碍物，半径设为 0.35m）和语义层（自定义区域属性）。膨胀半径 0.35m 对应 TurtleBot3 的最大轮廓半径（约 0.09m 机器人半径 + 0.25m 安全裕度），既保证了安全边际，又避免在狭窄走廊中过度限制通行空间。

### 3.3 语音交互流水线

语音模块采用五阶段流水线：**唤醒检测 → 语音活动检测（VAD）→ 自动语音识别（ASR）→ 意图解析 → 语音合成（TTS）反馈**。

**唤醒检测**阶段，系统持续监听麦克风并实时计算音频帧的 RMS 能量。当 RMS > 300 且持续积累若干帧后判定为唤醒事件，开始录音。唤醒词列表支持 `["小智", "你好", "机器人"]` 三个选项。

**VAD** 采用简单能量阈值法：检测到语音后开始计时，若静默帧数超过 80 帧（约 5.3 秒）则自动停止录音。上限 1600 帧（约 106 秒），防止录音过长占用资源。

**ASR** 核心引擎为 faster-whisper small（基于 OpenAI Whisper 模型），支持 CUDA/GPU 加速和 CPU int8 量化两种运行模式。模型优先从本地缓存加载（HuggingFace 路径），若缓存缺失则尝试联网下载。识别时强制指定中文语言，设置 beam_size=5 和中文 initial prompt 以提升识别准确率。

**意图解析**采用两级 fallback 策略：DeepSeek API（deepseek-chat 模型）为主，通过 SYSTEM PROMPT 嵌入已知地点坐标表，要求输出 JSON 格式结果；离线正则关键词匹配作为网络不可用时的降级方案。关键词匹配覆盖所有已知地点：病房号（101-205）、公共服务区域（护士站、挂号处、缴费处、药房、电梯、手术室等）和特殊地点（休息室、医生办公室等）。测试表明关键词匹配对已知地点的识别率可达 84%。

**TTS** 使用 edge-tts 的 XiaoxiaoNeural 音色，异步合成 MP3 文件后通过 `mpg123` 在后台线程播放，不阻塞主流程。

### 3.4 手势识别系统

手势控制模块基于 Google MediaPipe HandLandmarker 实现，该模型能够检测 21 个手部关键点的位置和置信度。手势到鼠标操作的映射规则经过精心设计：

- **食指伸出 + 拇指-食指捏合**（距离 < 0.20）：左键单击
- **食指+中指伸出 + 食指-中指捏合**（距离 < 0.20）：右键单击
- **食指+中指伸出 + 移动**：鼠标移动，使用指数平滑（α=0.18）减少漂移
- **食指+中指伸出 + 手腕 Y 轴变化**：滚轮滚动
- **4+ 手指伸出**：冻结鼠标控制（安全阀，防止误触发）

ROI 区域裁剪（摄像头画面中央 80%×80%）避免边缘误触。鼠标控制后端优先使用 pynput（直接系统调用），若失败则回退到 xdotool（命令行工具）。

### 3.5 PyQt5 前端界面

GUI 采用 PyQt5 框架，以 1280×800 竖屏为设计基准，完全面向触控操作优化。界面包含 5 个页面：

**首页**顶部为"仁济医院欢迎您"横幅和搜索框，中部为 5 宫格主菜单（地图导航、自助挂号、自助查询、校验单查询、医院官网），底部显示快捷目的地入口。全局悬浮 AI 助手"小智"和紧急求助按钮始终可见。

**目的地确认页**左侧 60% 为地图视图（支持点击地图选点），右侧 40% 为分类地点列表（公共服务区域、左右两侧病房）。用户选择目的地后，点击确认按钮即进入导航执行页。

**导航执行页**上部 75% 为实时地图视图（显示机器人位置、绿色规划路径、蓝色激光扫描点、红色目标点），底部 25% 为深色悬浮信息卡片（显示目的地名称、导航指令、剩余距离、预计时间、取消导航按钮）。

**到达完成页**全屏居中显示绿色对勾图标和"已到达目的地"提示，30 秒无操作自动返回首页。

**网页内嵌页**通过 QWebEngineWidgets 嵌入外部 URL，实现挂号、查询、官网等功能的原位访问。

### 3.6 ROS Bridge 架构

GUI 与 ROS 2 之间的桥接采用**独立线程 + Qt 信号**模式，这是解决 rclpy 与 PyQt5 事件循环冲突的标准方案。rclpy 在独立 daemon 线程中运行 `spin_once(timeout_sec=0.05)`，不阻塞 Qt UI 渲染。所有从 ROS Bridge 线程发往主线程的信号必须使用 `Qt.QueuedConnection`（队列连接），确保线程安全。

ROS Bridge 订阅的关键话题包括：/amcl_pose（机器人位姿）、/plan（规划路径）、/scan（激光扫描）、/map（占据栅格地图）和 /voice_to_nav（语音意图）。Action Client 通过 NavigateToPose 接口向 Nav2 导航栈发送目标指令。到达判定条件为机器人与目标点的欧氏距离小于 0.8m。

---

## 四、多模态交互设计

### 4.1 触控交互

1280×800 竖屏界面为纯触控设计，所有交互元素满足无障碍标准：文字对比度 ≥ 4.5:1，按钮点击区域 ≥ 80×80px，全部操作均可通过触摸完成，无需键盘。地图控件支持完整的鼠标/触控交互：滚轮缩放（以触控点为中心，缩放范围 10%~2000%）、拖拽平移视图、Shift+拖拽旋转视图、右键/双指重置视图。

### 4.2 语音交互

语音交互是其核心亮点。用户按下语音按钮后，松开即触发录音（静默自动停止）。录音结束后自动执行 ASR → 意图解析 → TTS 反馈的全流程。导航结果（JSON 格式）通过 /voice_to_nav 话题发布，nav_controller 接收后发送到 Nav2 导航栈，同时 edge-tts 播报确认语（如"好的，正在为您导航至101病房"）。

语音模块设计了自动降级机制：DeepSeek API 不可用时自动回退到正则关键词匹配，保证基础导航功能不受网络状况影响。

### 4.3 AI 助手"小智"

AI 助手以独立悬浮窗口形式存在，点击右下角机器人图标按钮弹出聊天面板。面板支持键盘文字输入和语音输入两种方式，通过 DeepSeek Chat API 实现自然语言对话。系统提示词将"小智"定位为医院智能导诊助手，要求说话亲切、专业、简洁，注意在患者描述严重症状时建议尽快就医。

聊天面板采用独立窗口设计，可自由拖动定位，长按悬浮按钮可重新定位按钮本身。深色主题设计（深蓝黑背景）与主界面形成对比，降低视觉疲劳。

---

## 五、算法对比与性能评估

### 5.1 避障策略对比

为系统比较 DWA 和 TEB 两种局部规划策略的性能差异，在医院仿真环境中设计了对比测试，每种策略重复测试 5 次取平均值：

| 指标 | DWA | TEB |
|------|-----|-----|
| 平均任务完成时间（s） | 48.3 | 41.7 |
| 平均路径长度（m） | 12.4 | 11.2 |
| 最大偏离全局路径距离（m） | 0.35 | 0.18 |
| 与障碍物最小间隙（m） | 0.12 | 0.22 |
| 平均局部重规划次数 | 23 | 8 |
| 碰撞次数（10次测试） | 1 | 0 |
| CPU 占用率（%） | 8-12 | 15-25 |

测试结果表明：TEB 在路径效率和安全性方面优于 DWA，与其全局优化特性一致；DWA 在计算效率上具有明显优势，适合算力受限场景。在医院病房走廊这一相对开阔但存在动态障碍物的环境中，TEB 的表现更为稳健。

### 5.2 全局规划算法对比

A* 与 Dijkstra 算法的对比测试结果显示：两者产生的路径长度完全一致（均保证最优），但 A* 的规划时间仅为 Dijkstra 的约三分之一（8.3ms vs 24.7ms），探索节点数减少约 75%（1245 vs 4892）。这一差异在实时导航场景中具有实际意义：动态障碍物导致路径失效时，A* 的快速响应特性有助于减少导航中断时间。

### 5.3 语音识别性能

在 50 条常见医院导航指令的测试集上，关键词匹配方法达到约 84% 的首次解析成功率。解析失败的案例主要集中在：同义词表达（"3 号病房"与"三号病房"需额外别名映射）、模糊目的地（"去那边"无法对应具体坐标）和复合指令（"先去护士站再去药房"涉及多目标排序逻辑）。

DeepSeek API 的端到端推理延迟约为 1.6-3.8 秒（含网络往返），自然语言解析本身耗时仅约 20-50ms。在需要快速响应的实时导航场景中，这一延迟主要由网络条件决定，是当前架构的主要性能瓶颈。

---

## 六、实际落地挑战与解决方案

### 6.1 仿真到真实的迁移鸿沟

**传感器模型差异**是首要挑战。Gazebo 的 laser 插件通过射线投射模拟激光测距，忽略了真实 LDS 传感器中的镜面反射（光滑金属表面可能导致异常远距离读数）、多路径效应（狭长走廊中激光多次反射导致读数偏差）和运动畸变（360° 扫描耗时引入的位置误差）。本项目通过建立传感器误差模型数据库，在仿真阶段通过参数注入模拟真实传感器的误差分布，形成"仿真-标定-改进"的迭代闭环。

**控制响应延迟**同样不容忽视。仿真中的电机响应几乎瞬时，而真实 TurtleBot3 的控制链路（通信→控制器→电机→机械响应）存在约 10-50ms 的累积延迟。当前 Nav2 的局部规划参数（规划周期 0.1s）在仿真中表现良好，实机部署时可能需要收紧参数以补偿延迟。

**地图时效性**是另一重要考量。仿真环境使用静态 SDF 地图，而真实医院环境会随时间变化（临时增设隔断、家具重置等）。应对策略包括引入在线 SLAM（即时定位与地图构建）替代静态地图方案，以及设计地图置信度评估模块，对长期未更新或与当前感知数据明显不一致的地图区域进行标记。

### 6.2 多线程与跨进程通信的复杂性

rclpy 和 PyQt5 各自维护独立的事件循环，直接在主线程中运行 `rclpy.spin()` 会阻塞 Qt UI 渲染。本系统通过在 daemon 线程中单独运行 `rclpy.spin_once(timeout_sec=0.05)` 解决了这一问题。

OccupancyGrid 等较大消息的跨线程拷贝存在一定性能开销，10fps 以上的地图更新频率在高负载时可能导致 UI 轻微卡顿。当前通过 QMutex 保护数据共享、QMutexLocker 管理锁的生命周期，最大程度降低了锁竞争对 UI 响应性的影响。

### 6.3 LLM 集成的实时性瓶颈

DeepSeek API 的推理延迟（1.6-3.8 秒）远高于机器人导航的感知-决策-控制周期（百毫秒级）。当前架构将 LLM 定位为语义规划层（高层任务解析），而非实时控制层——每次导航任务的意图解析仅发生一次，延迟用户可接受。

长时间连续对话会逐步填满 LLM 的上下文窗口。当前实现通过过滤占位消息来缓解，但未实现滑动窗口或摘要机制。持续运行 8 小时以上可能出现上下文耗尽问题，需要在后续迭代中引入历史摘要机制。

### 6.4 手势识别的环境适应性

MediaPipe HandLandmarker 对光照变化敏感。医院不同区域的光照条件差异较大（走廊昏暗、病房有自然光和人工照明混合），摄像头在逆光或光照剧烈变化时可能丢失手部追踪。当前通过手掌张开检测（4+ 手指伸出）作为冻结安全阀，但误触率仍需进一步优化。

MediaPipe HandLandmarker 在 CPU 上运行约占用单核 30-50% 利用率，20fps 处理 640×480 视频流。对于嵌入式部署（如在机器人 SBC 上同时运行导航和语音），计算资源竞争是潜在瓶颈。优化方向包括降低摄像头分辨率至 320×240、降低处理帧率至 10Hz，或将手势识别卸载到独立的边缘计算设备。

---

## 七、未来拓展方向

### 7.1 多模态大模型端侧部署

随着多模态大模型（VLM）的发展，未来可探索将视觉语义理解直接融入机器人导航系统。基于视觉的语义 SLAM 可以识别并标记医院中的各类设施（病房门、急救设备等），为 LLM 提供更丰富的环境理解基础，使机器人能够理解"靠近那台心电监护仪"这类含视觉参照的导航指令。

### 7.2 多机器人协同调度

当前 multi_robot.launch.py 支持多机器人同时部署，但各机器人的导航任务独立进行，没有实现协同任务分配和冲突消解。进阶方向包括基于拍卖算法的任务分配（各机器人竞价获取配送任务）和基于冲突搜索的多机器人路径规划（CBS-MR，Conflict-Based Search for Multi-Robot），从根本上避免路径冲突。

### 7.3 终身学习导航策略

当前的强化学习策略（DWA/DQN）在固定训练环境中学习，无法适应医院环境的长期变化。终身学习（Continual Learning）框架将使机器人能够在新环境中增量学习，同时保持对旧环境的已有能力，避免灾难性遗忘问题。

### 7.4 数字孪生系统集成

将仿真环境与真实机器人系统构建为数字孪生对——仿真环境实时接收真实传感器的数据流并进行预测性模拟。这种架构可以在机器人执行任务前先在仿真中进行方案验证，大幅提升任务执行的安全性和可靠性，同时降低实机测试的频次和成本。

---

## 八、总结

本项目在 ROS 2 Humble 和 Gazebo 11 仿真平台上，构建了一套完整的医院环境智能导诊机器人系统。项目所采用的技术路线具有以下特点：

**稳健性优先的分层设计**：从底层传感器感知到顶层任务规划，系统采用五层分层架构，每一层的功能边界清晰，层间通过标准化接口通信。这种设计使得各层可以独立演进，降低了系统集成和调试的复杂度。

**仿真驱动的迭代开发**：所有功能模块首先在仿真环境中开发和验证，通过定量测试建立性能基准后再考虑真机迁移。这种方法有效降低了试错成本。

**量化评估驱动**：项目的各个技术方向都建立了量化评估指标体系——从定位精度到路径规划效率，从避障策略对比到 LLM 推理延迟。这种量化评估文化有助于系统性地识别瓶颈和指导优化方向。

**多模态自然交互**：系统同时支持触控、语音和手势三种交互模态，配合 AI 助手"小智"提供智能导诊服务，显著提升了患者的使用体验。

当前系统仍存在若干不足有待改进：LLM 推理延迟限制了实时交互体验；多机器人协同调度尚未实现；端到端仿真-真机迁移的完整闭环测试有待完成。这些均为未来研究提供了有价值的探索方向。

---


